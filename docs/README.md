# Docs

当前有效的操作和部署文档。

| 文件 | 用途 |
|---|---|
| `supabase-setup.html` | Command Center 的 Supabase、GitHub Pages、本地测试部署指南 |

## 维护规则

- 新文档使用描述性文件名。
- 不再使用 `final`、`new`、`copy`、`original` 这类版本后缀。
- 旧文档由 Git 历史保存；当前目录只保留现在会用的文档。
